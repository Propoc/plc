{
  "name": "card-game-server",
  "version": "1.0.0",
  "description": "Backend server for card game",
  "main": "server.js",
  "scripts": {
    "build": "CI='' react-scripts build",
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
